# Images Directory

This directory contains bootable images for BlackboxOS.

- `blackboxos.iso`: Bootable ISO installer.
- `blackboxos.qcow2`: VM image for QEMU/VirtualBox.
- `blackboxos-ova`: OVA for VMware/VirtualBox.

To create: Run `./build/build-all.sh` on a Linux host.
